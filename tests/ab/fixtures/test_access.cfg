<?php
# Test access.cfg — pointed at by HZMETRICS_ACCESS_CFG when the A/B harness runs.
# Mirrors the shape of /etc/hubzero-metrics/access.cfg but routes every
# database operation to the test DB pair created by setup_test_dbs.sh.
$hub_dir   = '/tmp';
$hub_db    = 'foo_test';
$metrics_db = 'foo_metrics_test';
$db_host   = 'localhost';
$db_user   = '';
$db_pass   = 'TChJsr3g4Dzke8';
$db_prefix = 'jos_';
